# Ravel formatter / linter configuration.
# All values shown below are the built-in defaults; this file dogfoods the
# configuration discovery path and serves as a reference.

[format]
line-width = 80
indent-width = 2

[lint]
